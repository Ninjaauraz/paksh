# Python
__pycache__/
*.pyc
.venv/
venv/
env/

# local database (rebuilt by seed_demo.py / the pipeline)
paksh.db

# secrets
.env

# OS
.DS_Store
Thumbs.db

# NOTE: _site/ is the built static site. For Vercel we DO commit it
# (see DEPLOY.md), so it is intentionally NOT ignored here.
