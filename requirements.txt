fastapi>=0.110
uvicorn[standard]>=0.29
google-genai>=0.8
python-dotenv>=1.0
feedparser>=6.0
numpy>=1.26
# per-story social share cards (og_images.py, called by export_static.py):
Pillow>=10.0        # draws the 1200x630 card
fonttools>=4.40     # converts the brand woff2 fonts -> ttf in memory
brotli>=1.1         # fonttools needs it to read woff2
